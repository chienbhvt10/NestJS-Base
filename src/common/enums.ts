export enum SortValue {
  ASC = 'asc',
  DESC = 'desc',
}

export enum ROLE {
  ADMIN = 'ADMIN',
  CLIENT = 'CLIENT',
}

export enum USER_STATUS {
  NOT_ACTIVE = 'NOT_ACTIVE',
  ACTIVE = 'ACTIVE',
  IN_ACTIVE = 'IN_ACTIVE',
}
